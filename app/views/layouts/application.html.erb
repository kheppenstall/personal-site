<!DOCTYPE html>
<html>
  <head>
    <title>PersonalSite</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

    <body class= <%= "homepage" if @homepage %> <%= "projects" if @projects %> >
      <%= render partial: "navbar" %>
      <%= yield %>
  </body>
</html>
